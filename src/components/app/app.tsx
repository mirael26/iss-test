import * as React from "react";

import Header from "../header/header";

const App = (): JSX.Element => {
  return (
    <div className="app">
      <Header></Header>
    </div>
  );
};

export default App;